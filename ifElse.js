let age = 13;
if (age < 12) {
    console.log("Can see pg movie");
  } else if (age < 15) {
    console.log("Can see pg and 12");
  } else if (age < 18) {
    console.log("Can see pg, 12 and 15");
  } else {
    console.log("Can view all movies");
  }

  // == means they are the same value
  // ==== means they are the same value AND data type
  // ctrl and / comment out more than one line of code at a time

  